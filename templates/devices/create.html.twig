{% extends "base.html.twig" %}

{% block body %}
	{{ form_start(form) }}
	{{ form_row(form.type, {"attr": {"class": "form-control mb-2 mr-sm-2"}}) }}
	{{ form_row(form.ports, {"attr": {"class": "form-control mb-2 mr-sm-2"}}) }}
	{{ form_label(form.ports, "To add multiple ports, seperate with ';'") }}
	{{ form_row(form.submit, {"attr": {"class": "btn btn-primary mb-2"}}) }}
	{{ form_end(form) }}

	{% for message in messages %}
		<div class="alert alert-secondary" role="alert">{{ message }}</div>
	{% endfor %}
{% endblock %}
